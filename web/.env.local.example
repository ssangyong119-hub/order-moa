# 오더모아 web 환경변수 예시
# 실제 값은 web/.env.local 에 넣고, .env.local 은 절대 커밋하지 않습니다.
# (Supabase 연동은 후속 작업 단위에서 추가됩니다. 단위 1에서는 사용하지 않음.)

# 브라우저 노출 가능 (anon key)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 서버 전용. 절대 클라이언트 번들/Git에 노출 금지
# SUPABASE_SERVICE_ROLE_KEY=
