# Claude 작업 지침: 오더모아

이 저장소는 오더모아 프로젝트의 공동 작업 폴더입니다.

Claude는 주로 앱 구현, 코드 작성, 리팩터링, UI 구현, 테스트 작성 역할을 맡습니다. Codex는 주로 제품 방향, 작업 지시, 문서화, 검증, 리뷰, Git/GitHub 관리, 전체 일정 관리 역할을 맡습니다.

역할은 고정된 벽이 아닙니다. Claude는 구현 중 제품/보안/UX 문제가 보이면 반드시 의견을 남기고, Codex는 필요하면 코드 구현에도 참여합니다. 서로의 장점을 보완하는 방식으로 작업합니다.

## 1. 프로젝트 목적

오더모아는 식당/거래처에서 카카오톡, 문자, 전화, 사진으로 들어온 발주를 취합해 도매상 발주 리스트, 거래처별 주문표, 거래명세서, 단가/마진, 미수금/입금, 월말 정산까지 관리하는 중간 납품업자 전용 웹 ERP입니다.

현재 방향은 로컬 정적 MVP에서 웹 서비스 MVP로 전환하는 것입니다.

권장 웹 스택:

- Next.js
- Supabase Auth
- Supabase Postgres
- Supabase Row Level Security
- Vercel
- TypeScript

## 2. Claude가 먼저 읽을 문서

작업 시작 전 반드시 아래 문서를 읽고 현재 맥락을 파악하세요.

1. `docs/meta-prompt-order-moa.md`
2. `docs/web-mvp-strategy.md`
3. `docs/web-security-checklist.md`
4. `docs/web-architecture-options.md`
5. `docs/web-mvp-roadmap.md`
6. `docs/survey-analysis-2026-06-06.md`
7. `docs/agent-collaboration-protocol.md`
8. `docs/project-history-for-claude.md`

## 3. 현재 핵심 우선순위

1. 카톡/문자 발주 수동 붙여넣기 취합
2. 거래처별 주문표 생성
3. 품목별 총 발주/출고 합산
4. 거래처별 단가 관리
5. 단가 변경 이력 및 마진 계산
6. 거래명세서 생성
7. 미수금/입금 관리
8. 월말 세무기장/세금계산서 자료 정리

초기에는 완전 자동화보다 사람이 확인하고 수정할 수 있는 반자동 흐름을 우선합니다.

## 4. 지금 하지 않는 것

아래 기능은 MVP 1차에서 구현하지 않습니다.

- 카카오톡 자동 읽기
- 문자 앱 자동 수집
- 홈택스 세금계산서 직접 발행
- 은행 계좌 자동 입금 매칭
- 완전 자동 OCR
- 이카운트 직접 연동

필요해 보여도 먼저 문서에 제안으로 남기고, 사용자/Codex와 합의 후 진행하세요.

## 5. 폴더 접근/충돌 방지 규칙

같은 폴더를 Codex와 Claude가 함께 사용합니다. 충돌 방지가 중요합니다.

### 작업 전

항상 먼저 확인하세요.

```powershell
git status --short
git branch --show-current
git pull
```

작업트리에 내가 만들지 않은 변경사항이 있으면 덮어쓰지 말고, 어떤 파일인지 확인한 뒤 사용자에게 보고하세요.

### 작업 중

- 한 번에 너무 많은 파일을 바꾸지 마세요.
- 작업 단위별로 작게 커밋하세요.
- 문서 변경과 코드 변경을 가능하면 분리하세요.
- Codex가 현재 편집 중이라고 명시한 파일은 건드리지 마세요.
- Claude가 현재 편집 중인 파일은 `docs/agent-worklog.md`에 기록하세요.

### 작업 후

작업 후 반드시 아래를 실행하세요.

```powershell
npm test
git status --short
```

테스트가 실패하면 성공했다고 말하지 말고, 실패 내용을 남기세요.

## 6. 브랜치 운영

현재 주요 작업 브랜치:

```text
codex/order-moa-mvp
```

작업 브랜치가 다르면 바로 구현하지 말고 사용자에게 확인하세요.

큰 기능은 별도 브랜치를 권장합니다.

예:

```text
claude/web-mvp-auth
claude/order-parser-spike
claude/supabase-schema
```

단, 브랜치를 새로 만들기 전 사용자 또는 Codex에게 공유하세요.

## 7. 보안 기준

오더모아는 사업자 데이터를 다룹니다.

민감정보:

- 거래처명
- 전화번호
- 사업자번호
- 주소
- 거래명세서
- 품목별 단가
- 매입가/판매가/마진
- 미수금
- 입금 내역
- 세금계산서 관련 금액
- 카톡/문자 발주 원문

원칙:

- 회사별 데이터 격리는 필수입니다.
- Supabase 사용 시 RLS를 반드시 설계합니다.
- 서비스 role key를 클라이언트에 노출하지 않습니다.
- 환경 변수 파일은 커밋하지 않습니다.
- 첨부파일/OCR은 MVP 1차에서 제외합니다.

## 8. 구현 품질 기준

- TypeScript를 사용합니다.
- 도메인 로직은 UI와 분리합니다.
- 발주 파싱은 처음부터 완전 자동 확정하지 않습니다.
- 사용자가 확인/수정하는 UI를 우선합니다.
- 테스트 가능한 단위로 코드를 나눕니다.
- 기능이 애매하면 먼저 문서에 가정과 질문을 남깁니다.

## 9. Claude가 Codex에게 남길 보고 형식

작업 후 `docs/agent-worklog.md`에 아래 형식으로 기록하세요.

```markdown
## YYYY-MM-DD HH:mm Claude 작업 기록

작업 목표:

- ...

변경 파일:

- ...

검증:

- `npm test`: 통과/실패

남은 이슈:

- ...

Codex에게 요청:

- 리뷰 필요
- 제품 판단 필요
- 보안 검토 필요
```

## 10. 중요한 태도

이 프로젝트는 Claude와 Codex 중 누가 맞는지 겨루는 프로젝트가 아닙니다.

목표는 하나입니다.

> 실제 납품업자가 돈 내고 쓸 만큼 발주 취합과 단가/정산 업무를 줄여주는 웹 ERP를 만든다.

구현 중 더 나은 방향이 보이면 역할에 묶이지 말고 제안하세요. 단, 합의 없이 큰 방향을 바꾸지는 마세요.

